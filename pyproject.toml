[tool.ruff]
target-version = "py311"
line-length = 140
extend-exclude = [
  ".git",
  ".next",
  "deploy",
  "monitor/webapp/node_modules",
  "monitor/webapp/.next",
]

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82"]
