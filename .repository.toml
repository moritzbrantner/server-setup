schema_version = 1
id = "moritzbrantner/server-setup"
kind = "infrastructure"
status = "maintenance"
summary = "Host bootstrap, configuration, deployment migration, and verification tooling for self-hosted services."
depends_on = []
consumed_by = []
supersedes = []
replaced_by = []
