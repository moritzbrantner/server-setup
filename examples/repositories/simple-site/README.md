# simple-site

A zero-build static website example for the `server-setup` sandbox.

What it demonstrates:

- static `server.conf` discovery
- static Nginx deployment
- simple smoke-testable HTML and asset delivery

Expected deploy hostname in the sandbox:

- `simple.localhost`
