{
  "name": "complex-site",
  "domain": "app.localhost",
  "build_output": ".",
  "build": "npm ci --no-fund --no-audit",
  "command": "PORT=4002 API_BASE_URL=http://127.0.0.1:4001 npm run start",
  "port": 4002,
  "health_endpoint": "/healthz",
  "health_retries": 10,
  "health_interval_seconds": 1
}
