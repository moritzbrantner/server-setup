{
  "name": "complex-site",
  "domain": "app.localhost",
  "build_output": ".",
  "deploy_hooks": {
    "build": "npm ci --no-fund --no-audit"
  },
  "runtime": {
    "mode": "service",
    "command": "PORT=3000 API_BASE_URL=http://api.localhost:8080 npm run start",
    "working_dir": ".",
    "port": 3000,
    "health_endpoint": "/healthz",
    "health_retries": 10,
    "health_interval_seconds": 1
  },
  "service": {
    "name": "complex-site.service"
  },
  "nginx": {
    "www_redirect": false,
    "tls_hostnames": [
      "app.localhost"
    ]
  }
}
